# ============================================================================
# LLM API Keys
# ============================================================================
# API keys for various LLM providers
# OpenAI API key for GPT models (get from https://platform.openai.com)
OPENAI_API_KEY=sk-proj-...

# Anthropic API key for Claude models (get from https://console.anthropic.com)
ANTHROPIC_API_KEY=sk-ant-...

# OpenRouter API key for accessing multiple models (get from https://openrouter.ai)
OPENROUTER_API_KEY=sk-or-...


# ============================================================================
# Application Settings
# ============================================================================
# General application configuration
# Environment: development, staging, or production
ENVIRONMENT=development

# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Enable debug mode for detailed error messages and auto-reload
DEBUG=true


# ============================================================================
# API Configuration
# ============================================================================
# FastAPI server configuration
# Host to bind the API server (0.0.0.0 for all interfaces)
API_HOST=0.0.0.0

# Port for the API server
API_PORT=8000

# Auto-reload on code changes (development only)
API_RELOAD=true


# ============================================================================
# Sandbox Settings - DOCKER-FREE CONFIGURATION
# ============================================================================
# CRITICAL: These settings allow the platform to run without Docker

# Disable sandbox functionality entirely (set to false to disable)
SANDBOX_ENABLED=true

# Enable mock sandbox mode when Docker is not available
SANDBOX_MOCK_MODE=true

# Docker image to use for sandbox environments (only used when Docker is available)
SANDBOX_IMAGE=agent-sandbox:latest

# Maximum execution time in seconds
SANDBOX_TIMEOUT=30

# Memory limit for sandbox containers (e.g., 512m, 1g, 2g)
SANDBOX_MEMORY_LIMIT=512m

# CPU limit (number of cores, e.g., 0.5, 1.0, 2.0)
SANDBOX_CPU_LIMIT=1.0

# Enable gVisor for enhanced security isolation
SANDBOX_ENABLE_GVISOR=false

# Disable network access in sandbox
SANDBOX_NETWORK_DISABLED=true


# ============================================================================
# Database (for future use)
# ============================================================================
# Redis connection URL for caching and session storage
REDIS_URL=redis://localhost:6379


# ============================================================================
# Telemetry
# ============================================================================
# Enable telemetry collection for monitoring and analytics
TELEMETRY_ENABLED=false


# ============================================================================
# Security
# ============================================================================
# Secret key for signing tokens and encrypting sensitive data
# IMPORTANT: Change this in production! Generate with: openssl rand -hex 32
SECRET_KEY=your-secret-key-here-change-in-production